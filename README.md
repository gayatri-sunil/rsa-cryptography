# rsa-cryptography
Single-file RSA lab in Python that implements key generation, encryption/decryption and signing (with CRT), plus basic side-channel hardening (blinding + fixed-structure exponentiation), self-tests, and benchmarks.
